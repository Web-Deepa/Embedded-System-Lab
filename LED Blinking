#include<avr/io.h>
#define F_CPU 16000000 //definning frequency 
#include<util/delay.h>
Int main(void){
DDRB=0x03; //making pin 0 and pin1 as output.
while(1)
{
_dalay_ms(500);
PORTB=0x03; //sending output in port B
_delay_ms(500); 
PORTB=0x00; //turn off LED 
}
}
